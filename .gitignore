# Let's ignore the the lock files for the moment, we will version them when we'll be close to first launch :-)
/composer.lock
/yarn.lock

# Let's also ignore the files generated by Laravel Mix for now
/public/css
/public/js

# Laravel standard .gitignore:
/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
